#include <stdio.h>

/**
 * main - function of the task
 *
 * Return: Always 0.
 */

int main(void)
{
char i;

for (i = 'a'; i <= 'z'; i++)
putchar(i);

for (letter = 'A'; letter <= 'Z'; letter++)
putchar(i);
putchar('\n');

return (0);

}
